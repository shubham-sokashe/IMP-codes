# javaSourceCodeRepo
Repo. for java code
here one class have private constructor
so we cannot create an object of that class into another class
so to create an object of that class we have to write a method which will pass its paramter to constructor and will return object of that class
